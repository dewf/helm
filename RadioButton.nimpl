module RadioButton;

import AbstractButton;

opaque Handle extends AbstractButton.Handle {
    void setChecked(bool checked_);             // C# conflict, need to fix code generator to check against reserved words for any identifiers
}

Handle create();
