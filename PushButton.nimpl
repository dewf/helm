module PushButton;

import Widget;
import Signal;

opaque Handle extends Widget.Handle {
    void setText(string label);
    void onClicked(Signal.VoidDelegate handler);
}

Handle create(string label);
