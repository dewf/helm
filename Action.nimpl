module Action;

import Signal;

opaque Handle {
    string getText();
    void setEnabled(bool state);
    void setText(string text);
    void setSeparator(bool state);
    void onTriggered(Signal.BoolDelegate handler);
}

Handle create();
Handle create(string text);
