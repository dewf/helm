module LineEdit;

import Signal;
import Widget;

opaque Handle extends Widget.Handle {
    void setText(string str);
    void onTextEdited(Signal.StringDelegate handler);
    void onReturnPressed(Signal.VoidDelegate handler);

    // an event of our own
    void onLostFocus(Signal.VoidDelegate handler);
}

Handle create();
