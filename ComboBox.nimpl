module ComboBox;

import Widget;
import Signal;

opaque Handle extends Widget.Handle {
    void setItems(Array<string> items);
    void onCurrentIndexChanged(Signal.IntDelegate handler);
    void onCurrentTextChanged(Signal.StringDelegate handler);
}

Handle create();
