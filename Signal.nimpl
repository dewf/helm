module Signal;

// various delegate signatures, just so we don't have to keep redefining them

delegate void VoidDelegate();
delegate void BoolDelegate(bool b);
delegate void IntDelegate(int i);
delegate void StringDelegate(string s);
